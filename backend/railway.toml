[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
# Use Dockerfile ENTRYPOINT - override any dashboard startCommand
startCommand = "/app/entrypoint.sh"
